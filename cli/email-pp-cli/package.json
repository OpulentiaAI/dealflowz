{
  "name": "email-pp-cli",
  "version": "0.1.0",
  "description": "Token-efficient TypeScript CLI for the Gmail API. Agent-native flags, compound run-update-email command for Northcliff deal-flow process emails, local mirror. Printed in the style of cli-printing-press.",
  "type": "module",
  "bin": {
    "email-pp-cli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node dist/index.js --help && node scripts/test-offline.js && node scripts/test-endpoints.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
